query PageGetBySlug($slug: String!) {
	page(slug: $slug) {
		id
		slug
		title
		seoTitle
		seoDescription
		content
		created
		publishedAt
		pageType {
			id
			slug
			name
		}
		metadata {
			key
			value
		}
	}
}
